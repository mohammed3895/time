import ClockCard from "../shared/clock/ClockCard";

const LandingPage = () => {
  return (
    <div className="w-full h-dvh flex items-center justify-center p-4 md:p-8">
      <ClockCard />
    </div>
  );
};

export default LandingPage;
