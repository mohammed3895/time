import ClockCard from "../shared/clock/ClockCard";

const LandingPage = () => {
  return (
    <div className="w-full h-dvh flex items-center justify-center">
      <ClockCard />
    </div>
  );
};

export default LandingPage;
